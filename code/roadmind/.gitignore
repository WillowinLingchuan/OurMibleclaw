# ── Python ──
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# ── 文书处理中间产物 ──
# 注意：原始语料（corpus/raw）、三段切分结果（corpus/split）与要件真值
#      （corpus/truth）都要进版本控制 —— 它们是可复现的基础与交付物。
#      下面只忽略"跑出来的"缓存与派生产物。
corpus/cache/
*.tmp.json
*.log
